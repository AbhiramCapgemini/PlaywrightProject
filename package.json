{
  "name": "sample-project",
  "version": "1.0.0",
  "description": "Playwright API test suite — DummyJSON capstone",
  "scripts": {
    "pretest":        "node kill-port.js",
    "test":           "playwright test",
    "test:smoke":     "playwright test --grep @smoke",
    "test:regression":"playwright test --grep @regression",
    "test:api":       "playwright test --grep @api",
    "report":         "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node":      "^20.0.0",
    "dotenv":           "^17.3.1",
    "ts-node":          "^10.9.2",
    "typescript":       "^5.9.3"
  }
}
